PORT=3002

# Zotero OAuth 앱 등록 후 발급받는 값 (https://www.zotero.org/oauth/apps)
ZOTERO_CLIENT_KEY=
ZOTERO_CLIENT_SECRET=

# 서버가 외부에서 접속 가능한 주소 (OAuth 콜백 URL 구성에 사용).
# 로컬에서만 쓸 땐 비워두면 http://localhost:$PORT 를 기본값으로 씀.
APP_BASE_URL=
