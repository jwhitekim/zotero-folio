# memo

논문별 메모(child note)와 독립 메모(standalone note) 읽고 쓰기. 원문은
캐시하지 않고 항상 Zotero에서 라이브로 읽는다 — 자세한 구조는
[../../design.md](../../design.md) 참고.

## Requirements

(`docs-compliance` 스킬로 작성 — `### Requirement: <이름>` + SHALL
문장, `GIVEN/WHEN/THEN` 시나리오 형식)
