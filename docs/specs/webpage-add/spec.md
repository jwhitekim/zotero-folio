# webpage-add

웹페이지 URL을 새 Zotero `webpage` 아이템으로 추가. 생성 이후 서지정보는
수정하지 않는다(`contract.md` 절대 규칙).

## Requirements

(`docs-compliance` 스킬로 작성 — `### Requirement: <이름>` + SHALL
문장, `GIVEN/WHEN/THEN` 시나리오 형식)
