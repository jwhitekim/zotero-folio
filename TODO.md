# TODO

## 진행 중

## 완료
- [x] 로그인 페이지 배치 구조 최적화 — 균등 3칼럼(카피/사진/CTA) 구조를 비대칭 2존(헤드라인+사진 통합 본문 62% + 마진노트 스타일 CTA 사이드바 38%)으로 재배치. `docs/design-references.md` 8절에 이미 기록된 "브랜드 패널+로그인 카드 이분법 폐기" 원칙을 실제 구조에 반영. CTA 영역은 위아래 박스 테두리 대신 왼쪽 세로선 하나로 단순화, 사진은 독립 칼럼이 아닌 헤드라인 하위 요소로 종속. 색상/폰트는 기존 토큰(Library Cobalt, Maru Buri+Pretendard) 유지. 1180px/680px 반응형 분기 갱신, 빌드 통과. 실브라우저(특히 태블릿 폭) 재확인은 사용자 몫
- [x] 가이드 페이지 등장 애니메이션 레퍼런스 기반 재구현 — 기존 `motion-pending`/`motion-visible` 2클래스 구조(비커밋 상태로 남아있던 코드, 작성 주체 불명)를 `is-visible` 1클래스 토글로 단순화하고 `transition`을 기본 규칙에 배치하는 표준 스크롤 리빌 패턴으로 교체. 레퍼런스 6개 조사·비교(IntersectionObserver + CSS transition vs `animation-timeline: view()`) 후 전자 채택, 근거는 `docs/guide-scroll-reveal-research.md`에 기록. 문서는 doc-guard 검사 통과. 빌드 통과 확인, 실브라우저 재확인은 사용자 몫
- [x] 멀티유저 지원 구현 — DB를 로컬 SQLite에서 Supabase(Postgres)로 전환(service_role 키, 서버가 user_id로 직접 스코프, RLS 미사용). 로그인은 기존 Zotero OAuth 그대로 유지하고 express-session 쿠키로 요청별 유저 식별. `folio_users`/`folio_papers`/`folio_sync_state`(다른 앱과 프로젝트 공유로 `folio_` 접두사) 테이블 DDL은 `server/supabase/schema.sql`, 기존 로컬 캐시 이관은 `server/scripts/migrate-to-supabase.mjs`(`npm run migrate:supabase`, `--dry-run` 지원). 구현 중 발견한 버그 수정: 세션 쿠키 secure 플래그가 로컬 http에서 쿠키 발급을 막던 문제, 컬렉션 jsonb 배열 필터 400 오류, 검색어 LIKE 이스케이프 과다. `.claude/CLAUDE.md` 아키텍처 절 갱신 완료. 사용자가 Supabase 대시보드에서 schema.sql 실행 완료. 사용자 요청으로 folio-verifier 실기동 검증은 생략함 — 실사용 중 문제 생기면 재확인 필요
- [x] 가이드 페이지 등장 애니메이션 새로고침 시 깜빡임/미작동 수정 — Chrome 전용 CSS 네이티브 스크롤 애니메이션(`animation-timeline: view()`)과 JS `IntersectionObserver` 기반 애니메이션이 같은 `.steps > li` 요소를 두고 경합해, 새로고침마다 먼저 보였다 사라졌다 다시 나타나는 깜빡임이 결정적으로 재현되던 문제. `app.css`에서 `@supports (animation-timeline: view())` 블록 3개와 관련 키프레임(`guide-step-reveal`, `guide-finish-reveal`) 제거, JS 시스템 하나만 남김. 빌드 통과 확인. 브라우저 확장 도구 사용 제한으로 실브라우저 재확인은 사용자 본인이 진행 필요
- [x] 사용 가이드 페이지 추가 — TabBar 탭 대신 `/guide` 별도 엔드포인트로(CLAUDE.md의 웹 UI 탭 범위 유지, 어디서도 링크는 안 걸어둠, 직접 URL 접근용). Zotero와의 관계(Folio는 보조 도구, 저장/동기화는 Zotero가 담당)부터 설명하고 Papers/원문 뷰어(펜·형광펜·지우개)/나만의 메모/컬렉션/검색 순으로 정리. 로그인 여부와 무관하게 열람 가능. 로컬 서버로 실제 렌더링·복귀 링크 동작 확인 완료, 빌드 통과 — iframe `contentWindow`에 직접 붙인 터치 리스너(`iframeTouch`)에 의존하던 구조가 원인. 별도 브라우징 컨텍스트(iframe) 안에서 발생한 포인터 이벤트를 iOS Safari가 부모 리스너로 확실히 전달하지 않음(w3c/pointerevents#325, WebKit 이슈 149264). iframe에 `pointer-events: none`을 걸어 모든 터치/휠을 최상위 스크롤 박스(`.html-viewer-scroll`)로 떨어뜨리고 PDF와 동일한 구조(같은 문서 안 div에 제스처 처리)로 통일. 트레이드오프로 스냅샷 콘텐츠 위 텍스트 드래그 선택/링크 클릭은 막힘(사용 빈도 낮아 수용하기로 확인받음). 빌드 통과. 실기기(iPad Safari) 최종 확인 필요
- [x] 형광펜 스냅을 수평 고정에서 임의 방향 직선으로 변경 — 시작점 y 고정을 없애고 시작점~현재점을 잇는 직선(방향 무관)으로 스냅. 빌드 통과, `bc2eb7d` 푸시 완료
- [x] 태블릿용 프리핸드 형광펜 추가 — 펜/지우개와 상호 배타인 3번째 도구. 기존 필기(ink) 저장/삭제 파이프라인 그대로 재사용(새 annotation 타입·라우트 없음), 색으로 도구 구분(펜 `#1a1a1a` / 형광펜 `#ffd54f`). 드래그를 시작~끝 x, 시작 y 기준 수평 막대로 자동 스냅(실시간 미리보기도 동일), 굵기 3단계(8/12/18pt) 선택. 렌더링은 반투명 + multiply 혼합으로 텍스트 위에 자연스럽게 겹침. 지우개는 색 분기 없이 그대로 형광펜도 함께 지움. 서버 검증은 색 화이트리스트 + 색별 굵기 목록으로 확장. 빌드 통과, `94c094a` 푸시 완료. 실기기(터치/스타일러스) 확인 필요
- [x] 필기 지우개 모드 구현 — 펜과 상호 배타인 별도 토글 버튼, 드래그 경로에 닿는 스트로크를 확인 팝업 없이 즉시 삭제(한글 100자 기준 스트로크 300개+ 대비, 확인창 없는 이유). `findInkKeysAt`로 다중 스트로크 히트테스트, 삭제는 펜의 탭 삭제와 같은 `deleteInkWhenSaved` 경로 재사용, `erasedInDrag`로 같은 드래그 내 중복 삭제 방지. `touch-gestures.js`의 `isPenMode`를 `penMode || eraserMode`로 일반화. 빌드 통과, `f5c3ca3` 푸시 완료. 실기기(터치/스타일러스 드래그 지우기) 확인 필요
- [x] PDF 필기(ink) 기능 1차 구현 — 원문 툴바에 펜 토글 + 굵기 선택(얇음/보통/굵음), 색은 고정(`#1a1a1a`). 하이라이트와 같은 Zotero annotation 동기화 패턴(낙관적 쓰기, pending key, 공용 삭제 확인 팝업) 재사용. `/api/papers/:key/ink` 라우트 병렬 추가, `touch-gestures.js`에 펜 모드 시 한 손가락 팬 대신 그리기로 넘기는 분기 추가. 빌드 통과, `3a67880` 푸시 완료. 실기기(터치/스타일러스) 확인 필요
- [x] Zotero ink annotation 실제 데이터 형식 확인 — 사용자가 만든 샘플을 API로 조회해 확정. `annotationPosition = {pageIndex, width, paths}`, `width`는 굵기(스트로크 공통 1개), `paths`는 `number[][]`(스트로크별 평탄화 좌표). `docs/pdf-ink-annotation-research.md`에 기록, `d04b693` 푸시 완료
- [x] 작업 디렉터리를 프로젝트 루트로 고정하는 규칙 + 루트 빌드 스크립트 추가 — Stop 훅이 `cd web` 이후 작업 디렉터리 복귀 누락으로 상대경로를 못 찾아 실패한 사고 재발 방지. `.claude/CLAUDE.md`에 규칙 기록, 루트 `package.json`에 `npm run build`(재빌드용, 빠름) 추가
- [x] 모바일 하단 탭바 다크 모드 글자 안 보이는 문제 수정 — 탭바 배경(고정 흰 유리 재질)은 테마 무관 고정인데 아이콘/글자 색이 테마 변수(`--text-muted`/`--accent`)를 써서 다크 모드에서 밝은 글자가 밝은 배경과 겹쳐 안 보였음. `.tab-btn`/`.search-fab` 색상을 라이트 모드 값으로 고정. 빌드 통과 확인
- [x] 하이라이트 팝업/사파리 콜아웃 겹침 대응 — 팝업 아래-우선 배치(`442a5f7`)로 실질적 겹침 해결 확인, 복사 버튼 추가(`1d08d2b`), 의도대로 작동 안 한 선택-제거 코드는 정리(`489ad5d`). 실기기 확인 완료
- [x] 3번-3: 노트 바텀시트 전환(폰+세로 태블릿, 탭 전환 완전 대체) — 새 `NoteBottomSheet.svelte`(접힘/중간/완전펼침 3단 스냅, 드래그+탭+키보드 토글), `PaperDetailSplit.svelte`에서 `mobilePane`/탭 UI 제거하고 matchMedia로 데스크톱 분할뷰와 분기. 데스크톱/가로 태블릿 분할뷰는 미변경. 빌드 통과, `77bb00b` 푸시 완료. 실기기 드래그 체감 확인 필요
- [x] 3번-2: 하이라이트 팝업 터치 타겟 점검 — `@media (pointer: coarse)`로 터치 환경에서만 스와치 25→44px, 삭제 버튼 min-height 44px 등으로 확대, 데스크톱 마우스 크기는 그대로 유지. 빌드 통과, `3b61a60` 푸시 완료
- [x] 3번-1: 태블릿 가로모드 분할뷰 브레이크포인트 조정 — `orientation: portrait` 조건 추가해 아이패드 Pro 12.9" 세로(폭 1024px) 사각지대 해결, 가로 태블릿/데스크톱은 분할뷰 그대로 유지. 빌드 통과, `da28bbb` 푸시 완료
- [x] 태블릿 PDF 핀치 확대/축소 + 한 손가락 팬 구현 — `touch-action: none`으로 전환해 네이티브 팬과 JS 확대 보정이 경합하던 근본 원인 제거(`docs/pdf-touch-pinch-zoom.md`), 손가락 1개/2개를 하나의 핸들러(`touch-gestures.js`)로 통합 처리, 형광펜 드래그는 `window.getSelection()` 재사용으로 구분. 실기기(태블릿)에서 사용자 확인 완료, `4faf9d7` 푸시 완료
- [x] 로그인 페이지에 Folio 부연설명 추가, README 서브타이틀 추가, package.json(들)에 description 추가 — `LoginPage.svelte`에 "Zotero용 개인 논문 읽기·메모 공간" 서브타이틀(`login-brand-sub`, `app.css` 스타일 추가), `README.md` 제목 아래 같은 문구 + "포트폴리오 아님" 명시, `web/package.json`에 description 필드 추가(루트 `package.json`은 기존에 이미 있어 유지). 빌드 통과 확인
- [x] Alt+← 리스너가 재등록 안 되는 버그 수정 — `popstate`/`keydown`(Alt+←) 리스너를 `setupDone` effect에서 분리해, scrollContainer/viewerEl 변경과 무관하게 마운트 시 1회만 등록·언마운트 시만 해제되는 별도 `$effect`로 옮김(형광펜 리스너와 동일 패턴). `web/src/components/PdfViewer.svelte`, 빌드 통과 확인. 실제 앱 구동 검증은 아직 안 함
- [x] 맥 환경에서 PDF 참고문헌 뒤로가기 단축키(Alt+←/Option+←)가 안 먹는 문제 진단·수정 — 원인: 클릭된 참고문헌 링크(`<a>`)에 포커스가 남으면 맥이 ArrowLeft 키다운 자체를 페이지 JS로 안 보냄(사용자 실기기 콘솔 로그로 확인). 점프 직후 링크 blur 처리로 수정, 로컬 재현 테스트로 확인, `0afa3f4` 푸시 완료
- [x] Alt+← 단축키 리스너가 쓰다가 꺼지는 버그 원인 파악 — `PdfViewer.svelte`의 `setupDone` 게이트 `$effect`(874~1048줄) 안에 `popstate`/`keydown`(Alt+←) 리스너가 등록돼 있는데, 이 effect가 읽는 `scrollContainer`/`viewerEl`이 나중에 바뀌면 Svelte가 cleanup만 실행하고 본문은 `setupDone` 때문에 early return 해 리스너가 재등록되지 않음. 형광펜 리스너는 이미 같은 이유로 별도 effect로 분리돼 있으나(1059줄 근처) Alt+← 리스너는 아직 그대로 남아 같은 버그를 가짐. 수정 여부는 사용자 확인 대기

## 보류 (사용자 확인 필요)
